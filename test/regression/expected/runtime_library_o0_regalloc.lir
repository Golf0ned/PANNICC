main:  # 0 params, 20 stack bytes
.Lmain_entry:
        call    print  # ($67)
        movl    %eax, %r10d
        movl    %r10d, 4(%rsp)
        call    alloc  # ($4)
        movq    %rax, %r10
        movq    %r10, 12(%rsp)
        movq    12(%rsp), %r10
        movq    %r10, 8(%rsp)
        movq    8(%rsp), %r10
        call    dealloc  # (%r10)
        movl    %eax, %r10d
        movl    %r10d, 16(%rsp)
        movl    $0, (%rsp)
.Lmain_10:
        movl    (%rsp), %r10d
        movl    %r10d, %eax
        ret