while_basic:
        pushq   %rbx    # [virtual]
        pushq   %rsp    # [virtual]
        pushq   %rbp    # [virtual]
        pushq   %r12    # [virtual]
        pushq   %r13    # [virtual]
        pushq   %r14    # [virtual]
        pushq   %r15    # [virtual]

.while_basic_entry:
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]
        movl    $0, $4(%rsp)
        movl    $4(%rsp), %7
        movl    %7, (%rsp)

.while_basic_8:
        movl    $1, $8(%rsp)
        movl    $8(%rsp), %9
        cmpl    %9, $0
        jne     .while_basic_16

.while_basic_10:
        movl    (%rsp), %11
        movl    %11, $12(%rsp)
        movl    $2, $16(%rsp)
        movl    $12(%rsp), %12
        movl    $16(%rsp), %13
        leal    (%12,%13), %14
        movl    %14, $12(%rsp)
        movl    $12(%rsp), %15
        movl    %15, (%rsp)
        jmp     .while_basic_8

.while_basic_16:
        movl    (%rsp), %17
        movl    %17, $20(%rsp)
        movl    $20(%rsp), %18
        movl    %18, $24(%rsp)

.while_basic_19:
        movl    $24(%rsp), %20
        movq    %20, %rax
        addq    $28, %rsp    # [virtual]
        popq    %rbx    # [virtual]
        popq    %rsp    # [virtual]
        popq    %rbp    # [virtual]
        popq    %r12    # [virtual]
        popq    %r13    # [virtual]
        popq    %r14    # [virtual]
        popq    %r15    # [virtual]
        ret

while_complex:
        pushq   %rbx    # [virtual]
        pushq   %rsp    # [virtual]
        pushq   %rbp    # [virtual]
        pushq   %r12    # [virtual]
        pushq   %r13    # [virtual]
        pushq   %r14    # [virtual]
        pushq   %r15    # [virtual]

.while_complex_entry:
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]
        movl    $0, $4(%rsp)
        movl    $4(%rsp), %11
        movl    %11, (%rsp)
        movl    $100, $12(%rsp)
        movl    $12(%rsp), %12
        movl    %12, $8(%rsp)

.while_complex_13:
        movl    $8(%rsp), %14
        movl    %14, $16(%rsp)
        movl    $16(%rsp), %15
        cmpl    %15, $0
        jne     .while_complex_27

.while_complex_16:
        movl    (%rsp), %17
        movl    %17, $20(%rsp)
        movl    $5, $24(%rsp)
        movl    $20(%rsp), %18
        movl    $24(%rsp), %19
        leal    (%18,%19), %20
        movl    %20, $20(%rsp)
        movl    $20(%rsp), %21
        movl    %21, (%rsp)
        movl    $8(%rsp), %22
        movl    %22, $28(%rsp)
        movl    $1, $32(%rsp)
        movl    $28(%rsp), %23
        movl    $32(%rsp), %24
        phi     [%23] <-> %25    # [virtual]
        subl    %24, %25
        movl    %25, $28(%rsp)
        movl    $28(%rsp), %26
        movl    %26, $8(%rsp)
        jmp     .while_complex_13

.while_complex_27:
        movl    (%rsp), %28
        movl    %28, $36(%rsp)
        movl    $36(%rsp), %29
        movl    %29, $40(%rsp)

.while_complex_30:
        movl    $40(%rsp), %31
        movq    %31, %rax
        addq    $44, %rsp    # [virtual]
        popq    %rbx    # [virtual]
        popq    %rsp    # [virtual]
        popq    %rbp    # [virtual]
        popq    %r12    # [virtual]
        popq    %r13    # [virtual]
        popq    %r14    # [virtual]
        popq    %r15    # [virtual]
        ret

while_empty:
        pushq   %rbx    # [virtual]
        pushq   %rsp    # [virtual]
        pushq   %rbp    # [virtual]
        pushq   %r12    # [virtual]
        pushq   %r13    # [virtual]
        pushq   %r14    # [virtual]
        pushq   %r15    # [virtual]

.while_empty_entry:
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]

.while_empty_3:
        movl    $0, (%rsp)
        movl    (%rsp), %4
        cmpl    %4, $0
        jne     .while_empty_6

.while_empty_5:
        jmp     .while_empty_3

.while_empty_6:
        movl    $0, $4(%rsp)
        movl    $4(%rsp), %7
        movl    %7, $8(%rsp)

.while_empty_8:
        movl    $8(%rsp), %9
        movq    %9, %rax
        addq    $12, %rsp    # [virtual]
        popq    %rbx    # [virtual]
        popq    %rsp    # [virtual]
        popq    %rbp    # [virtual]
        popq    %r12    # [virtual]
        popq    %r13    # [virtual]
        popq    %r14    # [virtual]
        popq    %r15    # [virtual]
        ret

while_if:
        pushq   %rbx    # [virtual]
        pushq   %rsp    # [virtual]
        pushq   %rbp    # [virtual]
        pushq   %r12    # [virtual]
        pushq   %r13    # [virtual]
        pushq   %r14    # [virtual]
        pushq   %r15    # [virtual]

.while_if_entry:
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]
        movl    $200, $4(%rsp)
        movl    $4(%rsp), %7
        movl    %7, (%rsp)

.while_if_8:
        movl    $1, $8(%rsp)
        movl    $8(%rsp), %9
        cmpl    %9, $0
        jne     .while_if_17

.while_if_10:
        movl    (%rsp), %11
        movl    %11, $12(%rsp)
        movl    $12(%rsp), %12
        cmpl    %12, $0
        jne     .while_if_14

.while_if_13:
        jmp     .while_if_16

.while_if_14:
        movl    $500, $16(%rsp)
        movl    $16(%rsp), %15
        movl    %15, $24(%rsp)
        jmp     .while_if_19

.while_if_16:
        jmp     .while_if_8

.while_if_17:
        movl    $0, $20(%rsp)
        movl    $20(%rsp), %18
        movl    %18, $24(%rsp)

.while_if_19:
        movl    $24(%rsp), %20
        movq    %20, %rax
        addq    $28, %rsp    # [virtual]
        popq    %rbx    # [virtual]
        popq    %rsp    # [virtual]
        popq    %rbp    # [virtual]
        popq    %r12    # [virtual]
        popq    %r13    # [virtual]
        popq    %r14    # [virtual]
        popq    %r15    # [virtual]
        ret
