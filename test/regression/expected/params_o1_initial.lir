fun1:
        movl    %edi, %0

.fun1_entry:
        movl    %0, %1
        imull   %0, %1
        movq    %1, %rax
        ret

fun2:
        movl    %edi, %0
        movl    %esi, %1

.fun2_entry:
        call    fun1(%0)
        movl    %eax, %2
        leal    (,%0,2), %3
        movl    %3, %4
        imull   %1, %4
        leal    (%2,%4), %5
        call    fun1(%1)
        movl    %eax, %6
        leal    (%5,%6), %7
        movq    %7, %rax
        ret

fun3:
        movl    %edi, %0
        movl    %esi, %1
        movl    %edx, %2
        movl    %ecx, %3

.fun3_entry:
        call    fun1(%3)
        movl    %eax, %4
        movl    %0, %5
        imull   %4, %5
        movl    %1, %6
        imull   %3, %6
        leal    (%5,%6), %7
        leal    (%7,%2), %8
        movq    %8, %rax
        ret

call1:

.call1_entry:
        call    fun1($5)
        movl    %eax, %0
        movq    %0, %rax
        ret

call2:
        movl    %edi, %0
        movl    %esi, %1

.call2_entry:
        call    fun2(%0, %1)
        movl    %eax, %2
        movq    %2, %rax
        ret

call3:
        movl    %edi, %0
        movl    %esi, %1

.call3_entry:
        call    fun3(%0, %1, $1, $0)
        movl    %eax, %2
        movq    %2, %rax
        ret

call4:

.call4_entry:
        call    fun1($10)
        movl    %eax, %0
        movl    %0, %1
        subl    $100, %1
        movq    %1, %rax
        ret

super_fun:
        movl    %edi, %0
        movl    %esi, %1
        movl    %edx, %2
        movl    %ecx, %3
        movl    %r8d, %4
        movl    %r9d, %5
        movl    (stack 0), %6
        movl    (stack 1), %7

.super_fun_entry:
        leal    (%0,%1), %8
        leal    (%8,%2), %9
        leal    (%9,%3), %10
        leal    (%10,%4), %11
        leal    (%11,%5), %12
        leal    (%12,%6), %13
        leal    (%13,%7), %14
        movq    %14, %rax
        ret
