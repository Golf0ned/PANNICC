square:
        pushq   %rbx    # [virtual]
        pushq   %rsp    # [virtual]
        pushq   %rbp    # [virtual]
        pushq   %r12    # [virtual]
        pushq   %r13    # [virtual]
        pushq   %r14    # [virtual]
        pushq   %r15    # [virtual]
        movq    %rdi, %0

.square_entry:
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]
        subq    $4, %rsp    # [virtual]
        movl    %0, $12(%rsp)
        movl    $12(%rsp), %5
        movl    %5, (%rsp)
        movl    $12(%rsp), %6
        movl    %6, $4(%rsp)
        movl    (%rsp), %7
        movl    $4(%rsp), %8
        phi     [%7] <-> %9    # [virtual]
        imull   %8, %9
        movl    %9, (%rsp)
        movl    (%rsp), %10
        movl    %10, $8(%rsp)

.square_11:
        movl    $8(%rsp), %12
        movq    %12, %rax
        addq    $16, %rsp    # [virtual]
        popq    %rbx    # [virtual]
        popq    %rsp    # [virtual]
        popq    %rbp    # [virtual]
        popq    %r12    # [virtual]
        popq    %r13    # [virtual]
        popq    %r14    # [virtual]
        popq    %r15    # [virtual]
        ret
