while_basic:
        pushq   %rbx    # [virtual]
        pushq   %rsp    # [virtual]
        pushq   %rbp    # [virtual]
        pushq   %r12    # [virtual]
        pushq   %r13    # [virtual]
        pushq   %r14    # [virtual]
        pushq   %r15    # [virtual]

.while_basic_entry:

.while_basic_0:
        phi     [$0, %3] <-> %1    # [virtual]
        cmpl    $1, $0
        jne     .while_basic_4

.while_basic_2:
        phi     [%1] <-> %3    # [virtual]
        addl    $2, %3
        jmp     .while_basic_0

.while_basic_4:
        movq    %1, %rax
        popq    %rbx    # [virtual]
        popq    %rsp    # [virtual]
        popq    %rbp    # [virtual]
        popq    %r12    # [virtual]
        popq    %r13    # [virtual]
        popq    %r14    # [virtual]
        popq    %r15    # [virtual]
        ret

while_complex:
        pushq   %rbx    # [virtual]
        pushq   %rsp    # [virtual]
        pushq   %rbp    # [virtual]
        pushq   %r12    # [virtual]
        pushq   %r13    # [virtual]
        pushq   %r14    # [virtual]
        pushq   %r15    # [virtual]

.while_complex_entry:

.while_complex_0:
        phi     [$100, %5] <-> %1    # [virtual]
        phi     [$0, %4] <-> %2    # [virtual]
        cmpl    %1, $0
        jne     .while_complex_6

.while_complex_3:
        phi     [%2] <-> %4    # [virtual]
        addl    $5, %4
        phi     [%1] <-> %5    # [virtual]
        subl    $1, %5
        jmp     .while_complex_0

.while_complex_6:
        movq    %2, %rax
        popq    %rbx    # [virtual]
        popq    %rsp    # [virtual]
        popq    %rbp    # [virtual]
        popq    %r12    # [virtual]
        popq    %r13    # [virtual]
        popq    %r14    # [virtual]
        popq    %r15    # [virtual]
        ret

while_empty:
        pushq   %rbx    # [virtual]
        pushq   %rsp    # [virtual]
        pushq   %rbp    # [virtual]
        pushq   %r12    # [virtual]
        pushq   %r13    # [virtual]
        pushq   %r14    # [virtual]
        pushq   %r15    # [virtual]

.while_empty_entry:
        cmpl    $0, $0
        je      .while_empty_entry

.while_empty_0:
        movq    $0, %rax
        popq    %rbx    # [virtual]
        popq    %rsp    # [virtual]
        popq    %rbp    # [virtual]
        popq    %r12    # [virtual]
        popq    %r13    # [virtual]
        popq    %r14    # [virtual]
        popq    %r15    # [virtual]
        ret

while_if:
        pushq   %rbx    # [virtual]
        pushq   %rsp    # [virtual]
        pushq   %rbp    # [virtual]
        pushq   %r12    # [virtual]
        pushq   %r13    # [virtual]
        pushq   %r14    # [virtual]
        pushq   %r15    # [virtual]

.while_if_0:
        cmpl    $200, $0
        je      .while_if_entry

.while_if_1:
        jmp     .while_if_3

.while_if_entry:
        cmpl    $1, $0
        je      .while_if_0

.while_if_2:

.while_if_3:
        phi     [$0, $500] <-> %4    # [virtual]
        movq    %4, %rax
        popq    %rbx    # [virtual]
        popq    %rsp    # [virtual]
        popq    %rbp    # [virtual]
        popq    %r12    # [virtual]
        popq    %r13    # [virtual]
        popq    %r14    # [virtual]
        popq    %r15    # [virtual]
        ret
