first:

.first_entry:
        movq    $1, %eax
        ret

second:

.second_entry:
        call    first()
        movl    %eax, %0
        movl    %0, %1
        addl    $1, %1
        movq    %1, %eax
        ret

third:

.third_entry:
        call    second()
        movl    %eax, %0
        movl    %0, %1
        addl    $2, %1
        movq    %1, %eax
        ret

fourth:

.fourth_entry:
        call    third()
        movl    %eax, %0
        movl    %0, %1
        addl    $3, %1
        movq    %1, %eax
        ret

fifth:

.fifth_entry:
        call    fourth()
        movl    %eax, %0
        movl    %0, %1
        addl    $4, %1
        movq    %1, %eax
        ret

sixth:

.sixth_entry:
        call    fifth()
        movl    %eax, %0
        movl    %0, %1
        addl    $5, %1
        movq    %1, %eax
        ret

main:

.main_entry:
        call    sixth()
        movl    %eax, %0
        call    first()
        movl    %eax, %1
        movl    $0, %2
        addl    %1, %2
        call    second()
        movl    %eax, %3
        leal    (%2,%3), %4
        call    third()
        movl    %eax, %5
        leal    (%4,%5), %6
        call    fourth()
        movl    %eax, %7
        leal    (%6,%7), %8
        call    fifth()
        movl    %eax, %9
        leal    (%8,%9), %10
        call    sixth()
        movl    %eax, %11
        leal    (%10,%11), %12
        call    first()
        movl    %eax, %13
        leal    (%12,%13), %14
        call    second()
        movl    %eax, %15
        leal    (%14,%15), %16
        call    third()
        movl    %eax, %17
        leal    (%16,%17), %18
        call    fourth()
        movl    %eax, %19
        leal    (%18,%19), %20
        call    fifth()
        movl    %eax, %21
        leal    (%20,%21), %22
        call    sixth()
        movl    %eax, %23
        leal    (%22,%23), %24
        movq    %24, %eax
        ret
