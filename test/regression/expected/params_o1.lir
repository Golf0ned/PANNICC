fun1:
        pushq   %rbx    # [virtual]
        pushq   %rsp    # [virtual]
        pushq   %rbp    # [virtual]
        pushq   %r12    # [virtual]
        pushq   %r13    # [virtual]
        pushq   %r14    # [virtual]
        pushq   %r15    # [virtual]
        movq    %rdi, %0

.fun1_entry:
        phi     [%0] <-> %1    # [virtual]
        imull   %0, %1
        movq    %1, %rax
        popq    %rbx    # [virtual]
        popq    %rsp    # [virtual]
        popq    %rbp    # [virtual]
        popq    %r12    # [virtual]
        popq    %r13    # [virtual]
        popq    %r14    # [virtual]
        popq    %r15    # [virtual]
        ret

fun2:
        pushq   %rbx    # [virtual]
        pushq   %rsp    # [virtual]
        pushq   %rbp    # [virtual]
        pushq   %r12    # [virtual]
        pushq   %r13    # [virtual]
        pushq   %r14    # [virtual]
        pushq   %r15    # [virtual]
        movq    %rdi, %0
        movq    %rsi, %1

.fun2_entry:
        pushq   %rax    # [virtual]
        pushq   %rdi    # [virtual]
        pushq   %rsi    # [virtual]
        pushq   %rdx    # [virtual]
        pushq   %rcx    # [virtual]
        pushq   %r8    # [virtual]
        pushq   %r9    # [virtual]
        pushq   %r10    # [virtual]
        pushq   %r11    # [virtual]
        movslq  %0, %rdi
        call    fun1
        movq    %rax, %2
        popq    %rax    # [virtual]
        popq    %rdi    # [virtual]
        popq    %rsi    # [virtual]
        popq    %rdx    # [virtual]
        popq    %rcx    # [virtual]
        popq    %r8    # [virtual]
        popq    %r9    # [virtual]
        popq    %r10    # [virtual]
        popq    %r11    # [virtual]
        phi     [$2] <-> %3    # [virtual]
        imull   %0, %3
        phi     [%3] <-> %4    # [virtual]
        imull   %1, %4
        phi     [%2] <-> %5    # [virtual]
        addl    %4, %5
        pushq   %rax    # [virtual]
        pushq   %rdi    # [virtual]
        pushq   %rsi    # [virtual]
        pushq   %rdx    # [virtual]
        pushq   %rcx    # [virtual]
        pushq   %r8    # [virtual]
        pushq   %r9    # [virtual]
        pushq   %r10    # [virtual]
        pushq   %r11    # [virtual]
        movslq  %1, %rdi
        call    fun1
        movq    %rax, %6
        popq    %rax    # [virtual]
        popq    %rdi    # [virtual]
        popq    %rsi    # [virtual]
        popq    %rdx    # [virtual]
        popq    %rcx    # [virtual]
        popq    %r8    # [virtual]
        popq    %r9    # [virtual]
        popq    %r10    # [virtual]
        popq    %r11    # [virtual]
        phi     [%5] <-> %7    # [virtual]
        addl    %6, %7
        movq    %7, %rax
        popq    %rbx    # [virtual]
        popq    %rsp    # [virtual]
        popq    %rbp    # [virtual]
        popq    %r12    # [virtual]
        popq    %r13    # [virtual]
        popq    %r14    # [virtual]
        popq    %r15    # [virtual]
        ret

fun3:
        pushq   %rbx    # [virtual]
        pushq   %rsp    # [virtual]
        pushq   %rbp    # [virtual]
        pushq   %r12    # [virtual]
        pushq   %r13    # [virtual]
        pushq   %r14    # [virtual]
        pushq   %r15    # [virtual]
        movq    %rdi, %0
        movq    %rsi, %1
        movq    %rdx, %2
        movq    %rcx, %3

.fun3_entry:
        pushq   %rax    # [virtual]
        pushq   %rdi    # [virtual]
        pushq   %rsi    # [virtual]
        pushq   %rdx    # [virtual]
        pushq   %rcx    # [virtual]
        pushq   %r8    # [virtual]
        pushq   %r9    # [virtual]
        pushq   %r10    # [virtual]
        pushq   %r11    # [virtual]
        movslq  %3, %rdi
        call    fun1
        movq    %rax, %4
        popq    %rax    # [virtual]
        popq    %rdi    # [virtual]
        popq    %rsi    # [virtual]
        popq    %rdx    # [virtual]
        popq    %rcx    # [virtual]
        popq    %r8    # [virtual]
        popq    %r9    # [virtual]
        popq    %r10    # [virtual]
        popq    %r11    # [virtual]
        phi     [%0] <-> %5    # [virtual]
        imull   %4, %5
        phi     [%1] <-> %6    # [virtual]
        imull   %3, %6
        phi     [%5] <-> %7    # [virtual]
        addl    %6, %7
        phi     [%7] <-> %8    # [virtual]
        addl    %2, %8
        movq    %8, %rax
        popq    %rbx    # [virtual]
        popq    %rsp    # [virtual]
        popq    %rbp    # [virtual]
        popq    %r12    # [virtual]
        popq    %r13    # [virtual]
        popq    %r14    # [virtual]
        popq    %r15    # [virtual]
        ret

call1:
        pushq   %rbx    # [virtual]
        pushq   %rsp    # [virtual]
        pushq   %rbp    # [virtual]
        pushq   %r12    # [virtual]
        pushq   %r13    # [virtual]
        pushq   %r14    # [virtual]
        pushq   %r15    # [virtual]

.call1_entry:
        pushq   %rax    # [virtual]
        pushq   %rdi    # [virtual]
        pushq   %rsi    # [virtual]
        pushq   %rdx    # [virtual]
        pushq   %rcx    # [virtual]
        pushq   %r8    # [virtual]
        pushq   %r9    # [virtual]
        pushq   %r10    # [virtual]
        pushq   %r11    # [virtual]
        movslq  $5, %rdi
        call    fun1
        movq    %rax, %0
        popq    %rax    # [virtual]
        popq    %rdi    # [virtual]
        popq    %rsi    # [virtual]
        popq    %rdx    # [virtual]
        popq    %rcx    # [virtual]
        popq    %r8    # [virtual]
        popq    %r9    # [virtual]
        popq    %r10    # [virtual]
        popq    %r11    # [virtual]
        movq    %0, %rax
        popq    %rbx    # [virtual]
        popq    %rsp    # [virtual]
        popq    %rbp    # [virtual]
        popq    %r12    # [virtual]
        popq    %r13    # [virtual]
        popq    %r14    # [virtual]
        popq    %r15    # [virtual]
        ret

call2:
        pushq   %rbx    # [virtual]
        pushq   %rsp    # [virtual]
        pushq   %rbp    # [virtual]
        pushq   %r12    # [virtual]
        pushq   %r13    # [virtual]
        pushq   %r14    # [virtual]
        pushq   %r15    # [virtual]
        movq    %rdi, %0
        movq    %rsi, %1

.call2_entry:
        pushq   %rax    # [virtual]
        pushq   %rdi    # [virtual]
        pushq   %rsi    # [virtual]
        pushq   %rdx    # [virtual]
        pushq   %rcx    # [virtual]
        pushq   %r8    # [virtual]
        pushq   %r9    # [virtual]
        pushq   %r10    # [virtual]
        pushq   %r11    # [virtual]
        movslq  %0, %rdi
        movslq  %1, %rsi
        call    fun2
        movq    %rax, %2
        popq    %rax    # [virtual]
        popq    %rdi    # [virtual]
        popq    %rsi    # [virtual]
        popq    %rdx    # [virtual]
        popq    %rcx    # [virtual]
        popq    %r8    # [virtual]
        popq    %r9    # [virtual]
        popq    %r10    # [virtual]
        popq    %r11    # [virtual]
        movq    %2, %rax
        popq    %rbx    # [virtual]
        popq    %rsp    # [virtual]
        popq    %rbp    # [virtual]
        popq    %r12    # [virtual]
        popq    %r13    # [virtual]
        popq    %r14    # [virtual]
        popq    %r15    # [virtual]
        ret

call3:
        pushq   %rbx    # [virtual]
        pushq   %rsp    # [virtual]
        pushq   %rbp    # [virtual]
        pushq   %r12    # [virtual]
        pushq   %r13    # [virtual]
        pushq   %r14    # [virtual]
        pushq   %r15    # [virtual]
        movq    %rdi, %0
        movq    %rsi, %1

.call3_entry:
        pushq   %rax    # [virtual]
        pushq   %rdi    # [virtual]
        pushq   %rsi    # [virtual]
        pushq   %rdx    # [virtual]
        pushq   %rcx    # [virtual]
        pushq   %r8    # [virtual]
        pushq   %r9    # [virtual]
        pushq   %r10    # [virtual]
        pushq   %r11    # [virtual]
        movslq  %0, %rdi
        movslq  %1, %rsi
        movslq  $1, %rdx
        movslq  $0, %rcx
        call    fun3
        movq    %rax, %2
        popq    %rax    # [virtual]
        popq    %rdi    # [virtual]
        popq    %rsi    # [virtual]
        popq    %rdx    # [virtual]
        popq    %rcx    # [virtual]
        popq    %r8    # [virtual]
        popq    %r9    # [virtual]
        popq    %r10    # [virtual]
        popq    %r11    # [virtual]
        movq    %2, %rax
        popq    %rbx    # [virtual]
        popq    %rsp    # [virtual]
        popq    %rbp    # [virtual]
        popq    %r12    # [virtual]
        popq    %r13    # [virtual]
        popq    %r14    # [virtual]
        popq    %r15    # [virtual]
        ret

call4:
        pushq   %rbx    # [virtual]
        pushq   %rsp    # [virtual]
        pushq   %rbp    # [virtual]
        pushq   %r12    # [virtual]
        pushq   %r13    # [virtual]
        pushq   %r14    # [virtual]
        pushq   %r15    # [virtual]

.call4_entry:
        pushq   %rax    # [virtual]
        pushq   %rdi    # [virtual]
        pushq   %rsi    # [virtual]
        pushq   %rdx    # [virtual]
        pushq   %rcx    # [virtual]
        pushq   %r8    # [virtual]
        pushq   %r9    # [virtual]
        pushq   %r10    # [virtual]
        pushq   %r11    # [virtual]
        movslq  $10, %rdi
        call    fun1
        movq    %rax, %0
        popq    %rax    # [virtual]
        popq    %rdi    # [virtual]
        popq    %rsi    # [virtual]
        popq    %rdx    # [virtual]
        popq    %rcx    # [virtual]
        popq    %r8    # [virtual]
        popq    %r9    # [virtual]
        popq    %r10    # [virtual]
        popq    %r11    # [virtual]
        phi     [%0] <-> %1    # [virtual]
        subl    $100, %1
        movq    %1, %rax
        popq    %rbx    # [virtual]
        popq    %rsp    # [virtual]
        popq    %rbp    # [virtual]
        popq    %r12    # [virtual]
        popq    %r13    # [virtual]
        popq    %r14    # [virtual]
        popq    %r15    # [virtual]
        ret
