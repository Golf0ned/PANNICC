while_basic:  # 0 params, 12 stack bytes
.Lwhile_basic_entry:
        movl    $0, (%rsp)
.Lwhile_basic_3:
        movq    $1, %cond
        cmpq    $0, %cond
        jne     .Lwhile_basic_9
.Lwhile_basic_4:
.Lwhile_basic_5:
        movl    (%rsp), %6
        movl    %6, %7
        addl    $2, %7
        movl    %7, 4(%rsp)
        movl    4(%rsp), %8
        movl    %8, (%rsp)
        jmp     .Lwhile_basic_3
.Lwhile_basic_9:
.Lwhile_basic_10:
        movl    (%rsp), %11
        movl    %11, 8(%rsp)
.Lwhile_basic_12:
        movl    8(%rsp), %13
        movl    %13, %eax
        ret

while_complex:  # 0 params, 20 stack bytes
.Lwhile_complex_entry:
        movl    $0, (%rsp)
        movl    $100, 4(%rsp)
.Lwhile_complex_5:
        movl    4(%rsp), %6
        cmpl    $0, %6
        jne     .Lwhile_complex_15
.Lwhile_complex_7:
.Lwhile_complex_8:
        movl    (%rsp), %9
        movl    %9, %10
        addl    $5, %10
        movl    %10, 8(%rsp)
        movl    8(%rsp), %11
        movl    %11, (%rsp)
        movl    4(%rsp), %12
        movl    %12, %13
        subl    $1, %13
        movl    %13, 12(%rsp)
        movl    12(%rsp), %14
        movl    %14, 4(%rsp)
        jmp     .Lwhile_complex_5
.Lwhile_complex_15:
.Lwhile_complex_16:
        movl    (%rsp), %17
        movl    %17, 16(%rsp)
.Lwhile_complex_18:
        movl    16(%rsp), %19
        movl    %19, %eax
        ret

while_empty:  # 0 params, 4 stack bytes
.Lwhile_empty_entry:
.Lwhile_empty_1:
        movq    $0, %cond
        cmpq    $0, %cond
        jne     .Lwhile_empty_4
.Lwhile_empty_2:
.Lwhile_empty_3:
        jmp     .Lwhile_empty_1
.Lwhile_empty_4:
.Lwhile_empty_5:
        movl    $0, (%rsp)
.Lwhile_empty_6:
        movl    (%rsp), %7
        movl    %7, %eax
        ret

while_if:  # 0 params, 8 stack bytes
.Lwhile_if_entry:
        movl    $200, (%rsp)
.Lwhile_if_2:
        movq    $1, %cond
        cmpq    $0, %cond
        jne     .Lwhile_if_12
.Lwhile_if_3:
.Lwhile_if_4:
        movl    (%rsp), %5
        cmpl    $0, %5
        jne     .Lwhile_if_8
.Lwhile_if_6:
.Lwhile_if_7:
.Lwhile_if_11:
        jmp     .Lwhile_if_2
.Lwhile_if_12:
.Lwhile_if_13:
        movl    $0, 4(%rsp)
.Lwhile_if_14:
        movl    4(%rsp), %15
        movl    %15, %eax
        ret
.Lwhile_if_8:
.Lwhile_if_9:
        movl    $500, 4(%rsp)
        jmp     .Lwhile_if_14