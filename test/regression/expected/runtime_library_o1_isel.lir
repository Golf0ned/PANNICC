main:  # 0 params, 0 stack bytes
.Lmain_entry:
        call    alloc  # ($4)
        movq    %rax, %0
        movl    $67, (%0)
        movl    (%0), %1
        call    print  # (%1)
        movl    %eax, %2
        call    dealloc  # (%0)
        movl    %eax, %3
        movl    $0, %eax
        ret