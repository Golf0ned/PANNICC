main:  # 0 params, 0 stack bytes
.Lmain_entry:
        call    print  # ($67)
        movl    %eax, %0
        call    alloc  # ($4)
        movq    %rax, %1
        call    dealloc  # (%1)
        movl    %eax, %2
        movl    $0, %eax
        ret